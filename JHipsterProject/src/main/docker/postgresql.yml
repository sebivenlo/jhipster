version: '2'
services:
    esd-postgresql:
        image: postgres:9.6.5
        # volumes:
        #     - ~/volumes/jhipster/esd/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=esd
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
